# Moved to issue B (packed compact only)

Unpacked compact H=128/384 is **kernel-closed** on `quant_missing_impl_0913`
(dual legal `V<64>`). Packed compact H=128/384 is still live
**[vmi_compact_v128_residual](../../vmi_compact_v128_residual/)**.

Recorded originally: assert `hidden % 256 == 0`. `recorded.log` is kept here.
