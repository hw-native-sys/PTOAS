# Feature request

PTODSL cannot express a typed GM pointer table. The checked negative fixture
fails at the nested pointer type, forcing bulk stacking/unstacking and measured
ratios of 0.24–0.32 versus direct CCE. Add the bounds-checkable pointer-table
load and launcher support specified in `README.md`.
