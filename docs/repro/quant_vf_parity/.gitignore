outputs/
# Profiler droppings from local msprof runs
localhost.localdomain_*_ascend_pt/

# Keep the AscendC host binary for FR3 device bench (repo root ignores *.so).
!fixtures/fp32_block_quant_artifact/executable.so
