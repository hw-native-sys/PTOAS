outputs/
# Profiler droppings from local msprof runs
localhost.localdomain_*_ascend_pt/

# Keep AscendC ctypes launch lib for backlog FP32 device bench (repo root ignores *.so).
!backlog/fixtures/fp32_block_quant_artifact/libfp32_block_quant.so
