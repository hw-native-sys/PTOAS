# Pins

| Component | Pin |
|-----------|-----|
| PTOAS branch | `zjw/quant_vf_parity` (this tree) |
| Related fix already landed | `zjw/bf16_vmi_fixes` (bf16 `vabs` + G=4 `group_broadcast`) |
| CANN | `9.1.0-beta.3` |
| conda | `cann91_dev` |

Compile fixtures need only `pto-test-opt`, `ptoas`, and `bisheng`.
