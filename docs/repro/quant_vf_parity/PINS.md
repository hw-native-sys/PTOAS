# Pins

| Component | Pin |
|-----------|-----|
| PTOAS branch | `zjw/quant_vf_parity` (this tree) |
| Related fix (base of this package) | [PR #1145](https://github.com/hw-native-sys/PTOAS/pull/1145) — bf16 abs + group-4 broadcast (`zjw/bf16_vmi_fixes`) |
| CANN | `9.1.0-beta.3` |
| conda | `cann91_dev` |

Compile fixtures need only `pto-test-opt`, `ptoas`, and `bisheng`.
