# Fixtures — open blockers only

| Ask | Files |
|-----|--------|
| **1 (primary)** | `reference_asc_block_idx_vf.asc`, `current_vmi_block_idx_vf.pto`, `current_vmi_multibuf_expand_full.pto` |
| **2** | `reference_asc_dequant_dblbuf.asc`, `broken_vmi_dequant_dblbuf.{pto,ptodsl.py}`, `working_vmi_dequant_narrow.{pto,ptodsl.py}` |

On-device notes: [`RECORDED_DEVICE_NOTES.md`](RECORDED_DEVICE_NOTES.md).

Solved FP32 block-quant material lives under [`../backlog/`](../backlog/) —
**not a blocker now**.
