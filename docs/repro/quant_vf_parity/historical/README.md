# Historical fixtures (not package entrypoints)

Older AscendC/VMI compile fixtures and a superseded dyn-pipe attempt.
Kept for provenance of the live open-ask fixtures under `../fixtures/`.

Solved FP32 block-quant (former FR3) is under [`../backlog/`](../backlog/),
not here.

Do **not** call these from `../README.md` or the check script.

The pre-squash branch with product dump harnesses is
`zjw/quant_vf_parity-archive` (local archive for reference).
