---
phase: 01-a5vm-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase1/a5vm_vec_type.mlir
  - test/phase1/a5vm_load_op.mlir
  - test/phase1/a5vm_abs_op.mlir
  - test/phase1/a5vm_store_op.mlir
  - test/phase1/a5vm_backend_switch.mlir
  - test/phase1/a5vm_shared_dialects.mlir
  - test/phase1/run_phase1_checks.sh
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - A5VM-01
  - A5VM-02
  - A5VM-03
  - A5VM-04
must_haves:
  truths:
    - "Developers can run focused source tests for the new backend boundary and the minimum a5vm IR surface."
    - "Each Phase 1 requirement has a concrete automated command instead of a placeholder verification note."
    - "The backend-switch and shared-dialect expectations are encoded as executable fixtures before implementation starts."
  artifacts:
    - path: "test/phase1/a5vm_vec_type.mlir"
      provides: "256-byte a5vm vector type acceptance and rejection cases"
      min_lines: 20
    - path: "test/phase1/a5vm_load_op.mlir"
      provides: "a5vm.load assembly and verifier coverage"
      min_lines: 15
    - path: "test/phase1/a5vm_abs_op.mlir"
      provides: "a5vm.abs assembly and verifier coverage"
      min_lines: 15
    - path: "test/phase1/a5vm_store_op.mlir"
      provides: "a5vm.store assembly and verifier coverage"
      min_lines: 15
    - path: "test/phase1/a5vm_backend_switch.mlir"
      provides: "backend selection fixture for --pto-backend=a5vm"
      min_lines: 20
    - path: "test/phase1/a5vm_shared_dialects.mlir"
      provides: "shared arith/scf preservation fixture"
      min_lines: 20
    - path: "test/phase1/run_phase1_checks.sh"
      provides: "documented FileCheck invocation path for all phase1 fixtures"
      min_lines: 20
  key_links:
    - from: "test/phase1/run_phase1_checks.sh"
      to: "test/phase1/a5vm_backend_switch.mlir"
      via: "direct ptoas | FileCheck invocation"
      pattern: "a5vm_backend_switch\\.mlir"
    - from: "test/phase1/a5vm_vec_type.mlir"
      to: "./build/tools/ptoas/ptoas"
      via: "parser/verifier command"
      pattern: "RUN: .*ptoas .*a5vm_vec_type\\.mlir"
---

<objective>
Create the Phase 1 verification fixtures and runner before implementation work begins.

Purpose: Replace the current Wave 0 verification gaps with committed source tests that define the exact A5VM type, op, backend-switch, and shared-dialect behaviors.
Output: Six `test/phase1/*.mlir` fixtures plus one executable `run_phase1_checks.sh` helper.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@.planning/phases/01-a5vm-foundation/01-RESEARCH.md
@.planning/phases/01-a5vm-foundation/01-VALIDATION.md
@.planning/codebase/TESTING.md
@test/basic/empty_func.mlir
@tools/ptoas/ptoas.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 1 MLIR/FileCheck fixtures</name>
  <read_first>
    test/phase1/a5vm_vec_type.mlir
    test/phase1/a5vm_load_op.mlir
    test/phase1/a5vm_abs_op.mlir
    test/phase1/a5vm_store_op.mlir
    test/phase1/a5vm_backend_switch.mlir
    test/phase1/a5vm_shared_dialects.mlir
    test/basic/empty_func.mlir
    tools/ptoas/ptoas.cpp
    .planning/phases/01-a5vm-foundation/01-VALIDATION.md
  </read_first>
  <files>test/phase1/a5vm_vec_type.mlir, test/phase1/a5vm_load_op.mlir, test/phase1/a5vm_abs_op.mlir, test/phase1/a5vm_store_op.mlir, test/phase1/a5vm_backend_switch.mlir, test/phase1/a5vm_shared_dialects.mlir</files>
  <action>Create six new source tests under `test/phase1/` with `// RUN:` lines that use `./build/tools/ptoas/ptoas` and `FileCheck`. Encode these exact behaviors. In `a5vm_vec_type.mlir`, include legal cases `!a5vm.vec<64xf32>` and `!a5vm.vec<128xi16>` plus illegal cases `!a5vm.vec<32xf32>` and `!a5vm.vec<64xi64>` with expected diagnostics mentioning `expected exactly 256 bytes`. In `a5vm_load_op.mlir`, use one `a5vm.load` returning `!a5vm.vec<64xf32>` from a `memref<1024xf32>` base plus explicit metadata attributes `layout = "nd"`, `valid_rows = 32`, `valid_cols = 32`, and `domain = "gm"`. In `a5vm_abs_op.mlir`, use one `a5vm.abs` where operand and result are both `!a5vm.vec<64xf32>` and add one negative case with mismatched vector types. In `a5vm_store_op.mlir`, use one `a5vm.store` consuming `!a5vm.vec<64xf32>` and a `memref<1024xf32>` destination with metadata attributes `layout = "nd"` and `domain = "vec"`. In `a5vm_backend_switch.mlir`, drive the future backend with `--pto-backend=a5vm --a5vm-allow-unresolved --a5vm-unresolved-report=%t.unresolved -o -` and check for LLVM-like text markers `define void @abs_kernel_2d`, one `call` line for `llvm.hivm`, and one unresolved marker comment prefix `; A5VM-UNRESOLVED:`. In `a5vm_shared_dialects.mlir`, include `arith.addi`, one `scf.for`, and one `a5vm.abs`, then check the debug dump path with `--a5vm-print-ir` retains `arith.addi` and `scf.for` while the hardware-facing op appears as `a5vm.abs`.</action>
  <acceptance_criteria>
    rg -n 'a5vm\\.vec<64xf32>|a5vm\\.vec<128xi16>|expected exactly 256 bytes' test/phase1/a5vm_vec_type.mlir
    rg -n 'a5vm\\.load|layout = "nd"|valid_rows = 32|valid_cols = 32|domain = "gm"' test/phase1/a5vm_load_op.mlir
    rg -n 'a5vm\\.abs|mismatched vector types' test/phase1/a5vm_abs_op.mlir
    rg -n 'a5vm\\.store|layout = "nd"|domain = "vec"' test/phase1/a5vm_store_op.mlir
    rg -n '--pto-backend=a5vm|--a5vm-allow-unresolved|A5VM-UNRESOLVED|llvm\\.hivm' test/phase1/a5vm_backend_switch.mlir
    rg -n '--a5vm-print-ir|arith\\.addi|scf\\.for|a5vm\\.abs' test/phase1/a5vm_shared_dialects.mlir
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f test/phase1/a5vm_vec_type.mlir &amp;&amp; test -f test/phase1/a5vm_load_op.mlir &amp;&amp; test -f test/phase1/a5vm_abs_op.mlir &amp;&amp; test -f test/phase1/a5vm_store_op.mlir &amp;&amp; test -f test/phase1/a5vm_backend_switch.mlir &amp;&amp; test -f test/phase1/a5vm_shared_dialects.mlir &amp;&amp; rg -n "RUN: .*ptoas" test/phase1/*.mlir'</automated>
  </verify>
  <done>The repository contains six Phase 1 fixtures whose contents spell out the exact legal and illegal behaviors required for BACK-01, BACK-02, and A5VM-01 through A5VM-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add a documented runner for all Phase 1 checks</name>
  <read_first>
    test/phase1/run_phase1_checks.sh
    .planning/phases/01-a5vm-foundation/01-VALIDATION.md
    test/samples/runop.sh
  </read_first>
  <files>test/phase1/run_phase1_checks.sh</files>
  <action>Create `test/phase1/run_phase1_checks.sh` as an executable Bash script with `#!/usr/bin/env bash` and `set -euo pipefail`. Resolve the compiler binary as `./build/tools/ptoas/ptoas` and fail with `error: missing ./build/tools/ptoas/ptoas` if absent. Run these six commands in order: `./build/tools/ptoas/ptoas test/phase1/a5vm_vec_type.mlir 2>&1 | FileCheck test/phase1/a5vm_vec_type.mlir`, `./build/tools/ptoas/ptoas test/phase1/a5vm_load_op.mlir -o - | FileCheck test/phase1/a5vm_load_op.mlir`, `./build/tools/ptoas/ptoas test/phase1/a5vm_abs_op.mlir -o - | FileCheck test/phase1/a5vm_abs_op.mlir`, `./build/tools/ptoas/ptoas test/phase1/a5vm_store_op.mlir -o - | FileCheck test/phase1/a5vm_store_op.mlir`, `./build/tools/ptoas/ptoas --pto-backend=a5vm test/phase1/a5vm_backend_switch.mlir -o - | FileCheck test/phase1/a5vm_backend_switch.mlir`, and `./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase1/a5vm_shared_dialects.mlir -o /dev/null 2>&1 | FileCheck test/phase1/a5vm_shared_dialects.mlir`. Print `phase1 check: <filename>` before each command so failures identify the current case.</action>
  <acceptance_criteria>
    rg -n 'set -euo pipefail|missing \\./build/tools/ptoas/ptoas|phase1 check:' test/phase1/run_phase1_checks.sh
    rg -n 'a5vm_vec_type\\.mlir|a5vm_load_op\\.mlir|a5vm_abs_op\\.mlir|a5vm_store_op\\.mlir|a5vm_backend_switch\\.mlir|a5vm_shared_dialects\\.mlir' test/phase1/run_phase1_checks.sh
    test -x test/phase1/run_phase1_checks.sh
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -x test/phase1/run_phase1_checks.sh &amp;&amp; rg -n "phase1 check:|FileCheck test/phase1/" test/phase1/run_phase1_checks.sh'</automated>
  </verify>
  <done>Phase 1 has a committed, executable invocation path that runs every planned source test without relying on external lit configuration.</done>
</task>

</tasks>

<verification>
`bash -lc 'test -x test/phase1/run_phase1_checks.sh && rg -n "FileCheck test/phase1/" test/phase1/run_phase1_checks.sh && rg -n "RUN: .*ptoas" test/phase1/*.mlir'`
</verification>

<success_criteria>
Every Phase 1 requirement in the roadmap has a corresponding committed source fixture, and the repository contains one executable runner that spells out the exact verification commands for the wave.
</success_criteria>

<output>
After completion, create `.planning/phases/01-a5vm-foundation/01-a5vm-foundation-01-SUMMARY.md`
</output>
