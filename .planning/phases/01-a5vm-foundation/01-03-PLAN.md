---
phase: 01-a5vm-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - include/PTO/Transforms/A5VMTextEmitter.h
  - include/PTO/Transforms/Passes.h
  - include/PTO/Transforms/Passes.td
  - lib/PTO/Transforms/A5VMTextEmitter.cpp
  - lib/PTO/Transforms/CMakeLists.txt
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - BACK-01
  - BACK-02
must_haves:
  truths:
    - "Developers can select `a5vm` through an explicit CLI flag while the existing `emitc` path remains the default."
    - "The new backend emits LLVM-like text directly from A5VM instead of calling `emitc::translateToCpp` when `--pto-backend=a5vm` is selected."
    - "Developers can inspect backend IR, intrinsic-selection decisions, and unresolved mappings with explicit flags and a sidecar report."
    - "Shared `arith` and `scf` constructs remain in the debug IR instead of being forced into A5VM-specific operations."
  artifacts:
    - path: "include/PTO/Transforms/A5VMTextEmitter.h"
      provides: "A5VM emission options and API"
      contains: "A5VMEmissionOptions"
    - path: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      provides: "LLVM-like text emitter plus unresolved-report logic"
      min_lines: 180
    - path: "tools/ptoas/ptoas.cpp"
      provides: "backend selection and CLI flag wiring"
      contains: "--pto-backend"
  key_links:
    - from: "tools/ptoas/ptoas.cpp"
      to: "include/PTO/Transforms/A5VMTextEmitter.h"
      via: "translateA5VMModuleToText call in the final emission branch"
      pattern: "translateA5VMModuleToText"
    - from: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      to: "test/phase1/a5vm_backend_switch.mlir"
      via: "llvm.hivm call spelling and unresolved comment format"
      pattern: "A5VM-UNRESOLVED|llvm\\.hivm"
---

<objective>
Wire the backend selector and the thin textual A5VM emission path at the current final emission boundary.

Purpose: Satisfy the Phase 1 backend-boundary requirements without redesigning the pass pipeline and provide explicit developer diagnostics for unresolved mappings.
Output: A dedicated A5VM text emitter helper, new CLI flags, and the final `--pto-backend=a5vm` branch in `ptoas`.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@.planning/phases/01-a5vm-foundation/01-RESEARCH.md
@tools/ptoas/ptoas.cpp
@include/PTO/Transforms/Passes.h
@include/PTO/Transforms/Passes.td
@lib/PTO/Transforms/CMakeLists.txt
@lib/PTO/Transforms/PTOToEmitC.cpp
@test/phase1/a5vm_backend_switch.mlir
@test/phase1/a5vm_shared_dialects.mlir
@test/phase1/run_phase1_checks.sh

<interfaces>
From `include/PTO/Transforms/Passes.h`:
```c++
enum class PTOArch {
  A3,
  A5,
};
std::unique_ptr<Pass> createEmitPTOManualPass(PTOArch arch);
```

From `tools/ptoas/ptoas.cpp`:
```c++
static llvm::cl::opt<std::string> ptoTargetArch("pto-arch", ...);
pm.addPass(pto::createEmitPTOManualPass(pto::PTOArch::A3));
if (failed(emitc::translateToCpp(*module, cppOS, declareVariablesAtTop)))
  return 1;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated A5VM textual emitter with unresolved tracking</name>
  <read_first>
    include/PTO/Transforms/A5VMTextEmitter.h
    lib/PTO/Transforms/A5VMTextEmitter.cpp
    lib/PTO/Transforms/CMakeLists.txt
    lib/PTO/Transforms/PTOToEmitC.cpp
    include/PTO/IR/A5VM.h
    test/phase1/a5vm_backend_switch.mlir
  </read_first>
  <files>include/PTO/Transforms/A5VMTextEmitter.h, lib/PTO/Transforms/A5VMTextEmitter.cpp, lib/PTO/Transforms/CMakeLists.txt</files>
  <action>Create a standalone emitter helper instead of extending `PTOToEmitC.cpp`. In `A5VMTextEmitter.h`, declare `struct A5VMEmissionOptions { bool printIntrinsicSelections; bool allowUnresolved; std::string unresolvedReportPath; };` and `LogicalResult translateA5VMModuleToText(ModuleOp module, llvm::raw_ostream &os, const A5VMEmissionOptions &options, llvm::raw_ostream &diagOS);`. In `A5VMTextEmitter.cpp`, emit LLVM-like text with the exact top-level shape `; ModuleID = '<input>'`, `declare` lines for any selected intrinsic stubs, and `define void @...` functions. Translate `a5vm.load`, `a5vm.abs`, and `a5vm.store` into `call` lines whose callee names begin with `@llvm.hivm.` and are synthesized from the op family plus vector type metadata. When a mapping is missing, either fail with a diagnostic containing `op=`, `type=`, and `mapping=` or, if `allowUnresolved` is true, emit a comment starting with `; A5VM-UNRESOLVED:` and append the same record to the unresolved report file indicated by `unresolvedReportPath`. If `printIntrinsicSelections` is true, print one `A5VM intrinsic:` line per resolved op to the diagnostics stream. Add `A5VMTextEmitter.cpp` to `PTOTransforms` in `lib/PTO/Transforms/CMakeLists.txt`.</action>
  <acceptance_criteria>
    rg -n 'struct A5VMEmissionOptions|translateA5VMModuleToText' include/PTO/Transforms/A5VMTextEmitter.h
    rg -n 'A5VM-UNRESOLVED:|A5VM intrinsic:|@llvm\\.hivm\\.' lib/PTO/Transforms/A5VMTextEmitter.cpp
    rg -n 'A5VMTextEmitter\\.cpp' lib/PTO/Transforms/CMakeLists.txt
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "struct A5VMEmissionOptions|translateA5VMModuleToText" include/PTO/Transforms/A5VMTextEmitter.h &amp;&amp; rg -n "A5VM-UNRESOLVED:|A5VM intrinsic:|@llvm\\.hivm\\." lib/PTO/Transforms/A5VMTextEmitter.cpp &amp;&amp; rg -n "A5VMTextEmitter\\.cpp" lib/PTO/Transforms/CMakeLists.txt'</automated>
  </verify>
  <done>The repo contains a dedicated A5VM text emitter API and implementation that can emit LLVM-like text, log intrinsic selection, and persist unresolved mappings to a sidecar report.</done>
</task>

<task type="auto">
  <name>Task 2: Wire backend selection and developer diagnostics into ptoas</name>
  <read_first>
    tools/ptoas/ptoas.cpp
    include/PTO/Transforms/Passes.h
    include/PTO/Transforms/Passes.td
    include/PTO/Transforms/A5VMTextEmitter.h
    test/phase1/a5vm_backend_switch.mlir
    test/phase1/a5vm_shared_dialects.mlir
    test/phase1/run_phase1_checks.sh
  </read_first>
  <files>tools/ptoas/ptoas.cpp, include/PTO/Transforms/Passes.h, include/PTO/Transforms/Passes.td</files>
  <action>Update `tools/ptoas/ptoas.cpp` to introduce these exact flags: `--pto-backend=emitc|a5vm` with default `emitc`, `--a5vm-print-ir`, `--a5vm-print-intrinsics`, `--a5vm-allow-unresolved`, and `--a5vm-unresolved-report=<path>`. Keep the current `emitc` pipeline and output behavior unchanged when `--pto-backend=emitc`. When `--pto-backend=a5vm`, do not call `createEmitPTOManualPass` or `emitc::translateToCpp`; instead run the existing pre-backend passes, optionally dump the post-pass module to `llvm::errs()` when `--a5vm-print-ir` is set, then call `translateA5VMModuleToText(...)` and write its result to the existing `-o` stream. On unresolved mappings without `--a5vm-allow-unresolved`, exit non-zero with a developer-facing message that includes the op name, vector type, and mapping key. On success with `--a5vm-unresolved-report`, always create or overwrite the report file. Update `include/PTO/Transforms/Passes.h` and `include/PTO/Transforms/Passes.td` only as needed to keep declarations/registration aligned with the new backend path; do not redesign earlier passes or move shared `arith`/`scf` operations into A5VM.</action>
  <acceptance_criteria>
    rg -n '"pto-backend"|a5vm-print-ir|a5vm-print-intrinsics|a5vm-allow-unresolved|a5vm-unresolved-report' tools/ptoas/ptoas.cpp
    rg -n 'emitc::translateToCpp|translateA5VMModuleToText|createEmitPTOManualPass' tools/ptoas/ptoas.cpp
    rg -n 'A5VM|backend' include/PTO/Transforms/Passes.h include/PTO/Transforms/Passes.td
  </acceptance_criteria>
  <verify>
    <automated>bash -lc './build/tools/ptoas/ptoas --pto-backend=a5vm test/phase1/a5vm_backend_switch.mlir -o - | FileCheck test/phase1/a5vm_backend_switch.mlir &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase1/a5vm_shared_dialects.mlir -o /dev/null 2&gt;&amp;1 | FileCheck test/phase1/a5vm_shared_dialects.mlir'</automated>
  </verify>
  <done>`ptoas` exposes an explicit backend selector, keeps `emitc` as the default path, and routes `--pto-backend=a5vm` through the new text emitter plus diagnostics without redesigning the existing pass pipeline.</done>
</task>

</tasks>

<verification>
`bash test/phase1/run_phase1_checks.sh`
</verification>

<success_criteria>
Developers can run `ptoas --pto-backend=a5vm` and get LLVM-like text from the A5VM branch, inspect backend IR and intrinsic choices with flags, and receive explicit unresolved markers instead of silent guesses.
</success_criteria>

<output>
After completion, create `.planning/phases/01-a5vm-foundation/01-a5vm-foundation-03-SUMMARY.md`
</output>
