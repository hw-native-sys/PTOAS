---
phase: 01-a5vm-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - include/PTO/IR/A5VM.h
  - include/PTO/IR/A5VMDialect.h
  - include/PTO/IR/A5VMDialect.td
  - include/PTO/IR/A5VMOps.td
  - include/PTO/IR/A5VMTypes.td
  - include/PTO/IR/CMakeLists.txt
  - lib/PTO/IR/A5VM.cpp
  - lib/PTO/IR/CMakeLists.txt
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - A5VM-01
  - A5VM-02
  - A5VM-03
  - A5VM-04
must_haves:
  truths:
    - "Developers can spell A5VM vector values as `!a5vm.vec<...>` and illegal widths are rejected."
    - "Developers can express the `Abs` path with `a5vm.load`, `a5vm.abs`, and `a5vm.store` operations."
    - "The CLI can parse modules containing the new A5VM dialect without relying on the future backend selector work."
  artifacts:
    - path: "include/PTO/IR/A5VMTypes.td"
      provides: "A5VM vector type contract"
      contains: "A5VM_VecType"
    - path: "include/PTO/IR/A5VMOps.td"
      provides: "load/abs/store op definitions"
      contains: "def A5VM_LoadOp"
    - path: "lib/PTO/IR/A5VM.cpp"
      provides: "type and op verifier implementation"
      min_lines: 120
    - path: "tools/ptoas/ptoas.cpp"
      provides: "A5VM dialect registration for parsing tests"
      contains: "A5VMDialect"
  key_links:
    - from: "include/PTO/IR/A5VMOps.td"
      to: "lib/PTO/IR/A5VM.cpp"
      via: "generated declarations plus handwritten verifiers"
      pattern: "A5VM_(Load|Abs|Store)Op"
    - from: "tools/ptoas/ptoas.cpp"
      to: "include/PTO/IR/A5VM.h"
      via: "dialect registration and getOrLoadDialect"
      pattern: "A5VM"
---

<objective>
Add the first-class A5VM dialect surface required for Phase 1.

Purpose: Establish a verified `a5vm` IR model with the exact vector type and `load`/`abs`/`store` operations that later backend work will emit.
Output: New A5VM IR headers/TableGen inputs, implementation, build hooks, and CLI dialect registration.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@.planning/phases/01-a5vm-foundation/01-RESEARCH.md
@include/PTO/IR/PTO.h
@include/PTO/IR/PTODialect.h
@include/PTO/IR/PTOOps.td
@include/PTO/IR/CMakeLists.txt
@lib/PTO/IR/CMakeLists.txt
@lib/PTO/IR/PTO.cpp
@tools/ptoas/ptoas.cpp

<interfaces>
From `include/PTO/IR/PTO.h`:
```c++
namespace mlir {
namespace pto {
AddressSpaceAttr getPTOAddressSpaceAttr(Type type);
bool isScalarPtrOrMemRef(Type type);
} // namespace pto
} // namespace mlir
```

From `tools/ptoas/ptoas.cpp`:
```c++
DialectRegistry registry;
registry.insert<mlir::pto::PTODialect>();
context.getOrLoadDialect<mlir::pto::PTODialect>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define A5VM dialect contracts and build hooks</name>
  <read_first>
    include/PTO/IR/A5VM.h
    include/PTO/IR/A5VMDialect.h
    include/PTO/IR/A5VMDialect.td
    include/PTO/IR/A5VMOps.td
    include/PTO/IR/A5VMTypes.td
    include/PTO/IR/CMakeLists.txt
    lib/PTO/IR/CMakeLists.txt
    include/PTO/IR/PTO.h
    include/PTO/IR/PTODialect.h
    include/PTO/IR/PTOOps.td
  </read_first>
  <files>include/PTO/IR/A5VM.h, include/PTO/IR/A5VMDialect.h, include/PTO/IR/A5VMDialect.td, include/PTO/IR/A5VMOps.td, include/PTO/IR/A5VMTypes.td, include/PTO/IR/CMakeLists.txt, lib/PTO/IR/CMakeLists.txt</files>
  <action>Create a new `a5vm` dialect module under `include/PTO/IR/` and build it with the existing TableGen pattern. Use the dialect namespace `a5vm` in assembly syntax and the C++ namespace `mlir::pto::a5vm`. In `A5VMTypes.td`, define exactly one public type `A5VM_VecType` printed as `!a5vm.vec&lt;{elementCount}x{elementType}&gt;`. In `A5VMOps.td`, define exactly three ops named `a5vm.load`, `a5vm.abs`, and `a5vm.store`. Make `a5vm.load` return `!a5vm.vec&lt;...&gt;` and take operands `(base, offset)` plus string/integer attributes `layout`, `domain`, `valid_rows`, and `valid_cols`. Make `a5vm.abs` take one vector operand and return one vector result of the same type. Make `a5vm.store` take operands `(value, base, offset)` plus string attributes `layout` and `domain`, with no result. Update `include/PTO/IR/CMakeLists.txt` to generate `A5VMDialect.h.inc`, `A5VMDialect.cpp.inc`, `A5VMOps.h.inc`, `A5VMOps.cpp.inc`, `A5VMTypes.h.inc`, and `A5VMTypes.cpp.inc`. Update `lib/PTO/IR/CMakeLists.txt` to compile `A5VM.cpp` into `PTOIR` and keep the include directories/export behavior unchanged.</action>
  <acceptance_criteria>
    rg -n 'A5VM_VecType|!a5vm\\.vec<' include/PTO/IR/A5VMTypes.td
    rg -n 'def A5VM_LoadOp|def A5VM_AbsOp|def A5VM_StoreOp|a5vm\\.load|a5vm\\.abs|a5vm\\.store' include/PTO/IR/A5VMOps.td
    rg -n 'A5VMDialect\\.h\\.inc|A5VMOps\\.h\\.inc|A5VMTypes\\.h\\.inc' include/PTO/IR/CMakeLists.txt
    rg -n 'A5VM\\.cpp' lib/PTO/IR/CMakeLists.txt
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "A5VM_VecType|!a5vm\\.vec<" include/PTO/IR/A5VMTypes.td &amp;&amp; rg -n "def A5VM_(Load|Abs|Store)Op" include/PTO/IR/A5VMOps.td &amp;&amp; rg -n "A5VM\\.cpp" lib/PTO/IR/CMakeLists.txt'</automated>
  </verify>
  <done>The source tree contains a buildable A5VM dialect contract with one vector type and exactly three ops, all registered through the repo’s standard IR TableGen structure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement A5VM parsing, printing, and verifiers</name>
  <read_first>
    lib/PTO/IR/A5VM.cpp
    tools/ptoas/ptoas.cpp
    include/PTO/IR/A5VM.h
    include/PTO/IR/A5VMDialect.h
    include/PTO/IR/A5VMOps.td
    include/PTO/IR/A5VMTypes.td
    lib/PTO/IR/PTO.cpp
    test/phase1/a5vm_vec_type.mlir
    test/phase1/a5vm_load_op.mlir
    test/phase1/a5vm_abs_op.mlir
    test/phase1/a5vm_store_op.mlir
  </read_first>
  <files>lib/PTO/IR/A5VM.cpp, tools/ptoas/ptoas.cpp, include/PTO/IR/A5VM.h, include/PTO/IR/A5VMDialect.h</files>
  <action>Implement the generated A5VM dialect in `lib/PTO/IR/A5VM.cpp`. Enforce the vector-width rule with this exact calculation: `elementCount * elementType.getIntOrFloatBitWidth() == 2048`, and emit diagnostics containing the phrase `expected exactly 256 bytes` when the rule fails. Verify `a5vm.abs` requires identical operand and result vector types. Verify `a5vm.load` result type is `!a5vm.vec&lt;...&gt;` and requires `valid_rows` and `valid_cols` integer attributes plus `layout` and `domain` string attributes. Verify `a5vm.store` consumes a legal `!a5vm.vec&lt;...&gt;` value and requires `layout` and `domain` string attributes. Update `tools/ptoas/ptoas.cpp` to include `PTO/IR/A5VM.h`, register `mlir::pto::a5vm::A5VMDialect` in the `DialectRegistry`, and call `context.getOrLoadDialect&lt;mlir::pto::a5vm::A5VMDialect&gt;()` so Phase 1 fixtures can parse before backend-selection work lands.</action>
  <acceptance_criteria>
    rg -n 'expected exactly 256 bytes|2048' lib/PTO/IR/A5VM.cpp
    rg -n 'valid_rows|valid_cols|layout|domain' lib/PTO/IR/A5VM.cpp
    rg -n 'mismatched|same type|a5vm\\.abs' lib/PTO/IR/A5VM.cpp
    rg -n 'A5VMDialect|getOrLoadDialect<mlir::pto::a5vm::A5VMDialect>' tools/ptoas/ptoas.cpp
  </acceptance_criteria>
  <verify>
    <automated>bash -lc './build/tools/ptoas/ptoas test/phase1/a5vm_vec_type.mlir 2&gt;&amp;1 | FileCheck test/phase1/a5vm_vec_type.mlir &amp;&amp; ./build/tools/ptoas/ptoas test/phase1/a5vm_load_op.mlir -o - | FileCheck test/phase1/a5vm_load_op.mlir &amp;&amp; ./build/tools/ptoas/ptoas test/phase1/a5vm_abs_op.mlir -o - | FileCheck test/phase1/a5vm_abs_op.mlir &amp;&amp; ./build/tools/ptoas/ptoas test/phase1/a5vm_store_op.mlir -o - | FileCheck test/phase1/a5vm_store_op.mlir'</automated>
  </verify>
  <done>The CLI can parse and verify A5VM vector types and the three minimum ops, and all four A5VM-specific Phase 1 fixtures pass through `ptoas` plus `FileCheck`.</done>
</task>

</tasks>

<verification>
`bash -lc './build/tools/ptoas/ptoas test/phase1/a5vm_vec_type.mlir 2>&1 | FileCheck test/phase1/a5vm_vec_type.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_load_op.mlir -o - | FileCheck test/phase1/a5vm_load_op.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_abs_op.mlir -o - | FileCheck test/phase1/a5vm_abs_op.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_store_op.mlir -o - | FileCheck test/phase1/a5vm_store_op.mlir'`
</verification>

<success_criteria>
`a5vm` exists as a first-class MLIR dialect in the PTO tree, the 256-byte vector rule is enforced by verifiers, and the minimum load/abs/store surface is parseable and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-a5vm-foundation/01-a5vm-foundation-02-SUMMARY.md`
</output>
